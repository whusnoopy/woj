#labels Phase-Design
= Introduction =
This [WikiPage] is DataBase Design Doc, described the details about the DataBase used in this project. 

= DataBase Description =

Database name:  flood

|| *Table Name* || *Description* || *Prime Index* ||
|| User || 用户信息 || user_id ||
|| Admin || 管理员信息 || NULL ||
|| [Login_Log] || 登录信息 || user_id ||
|| Mail || 邮件信息 || mail_id ||
|| Message || 讨论信息 || message_id ||
|| [Source_Code] || 源代码信息 || submit_id ||
|| Submit || 提交信息 || submit_id ||
|| Problem || 题目信息 || problem_id ||
|| Contest || 比赛信息 || contest_id ||
|| [Contest_Problem] || 比赛与题目关联信息 || contest_id ||
|| [Contest_User] || 内部比赛的参赛人员列表 || contest_id ||


= Table Details =

== User ==
|| *Field* || *Type* || *Null* || *Key* || *Default* || *Extra* ||
|| user_id || varchar(16) || NO || PRI || || ||
|| password || text || NO || || || ||
|| email || varchar(64) || YES || || || ||
|| school || varchar(64) || NO || || || ||
|| nickname || varchar(64) || NO || || || ||
|| reg_time || datatime || NO || || || ||